---
phase: 01-infrastructure-data-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
autonomous: false
human_checkpoint: true
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/cd.yml
  - railway.toml
requirements:
  - FR-1.3
  - FR-1.4
---

<objective>
Create the CI/CD pipeline and Railway deployment configuration so every PR gets lint+test+build validation, every main merge auto-deploys, and the Railway stack runs with a persistent PostgreSQL+TimescaleDB volume that survives service restarts.

FR-1.3: Railway deployment runs; schema migration survives restart.
FR-1.4: GitHub Actions CI runs on PR; merge to main triggers auto-deploy.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions CI workflow (PR validation)</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Create `.github/workflows/ci.yml`:
- Trigger: `on: pull_request`
- Services: postgres (timescale/timescaledb:latest-pg15), redis
- Steps: checkout, python setup, pip install, ruff lint, pytest (backend), docker buildx build (all 4 services)
- DB health check before pytest: `pg_isready -h localhost -U pead`
- rl_trainer service excluded from Docker build step (separate job to ensure separation)
  </action>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions CD workflow (main → Railway auto-deploy)</name>
  <files>.github/workflows/cd.yml</files>
  <action>
Create `.github/workflows/cd.yml`:
- Trigger: `on: push: branches: [main]`
- Uses Railway CLI action to trigger deploy
- Excludes rl_trainer from auto-deploy (CLAUDE.md: rl_trainer is manual-deploy-only)
- Requires secret: RAILWAY_TOKEN
  </action>
</task>

<task type="auto">
  <name>Task 3: railway.toml configuration</name>
  <files>railway.toml</files>
  <action>
Create `railway.toml` specifying services, build commands, start commands, health check paths.
rl_trainer service: deployTrigger = "manual" (CLAUDE.md directive).
  </action>
</task>

<task type="human_checkpoint">
  <name>Task 4: Railway project setup (human action required)</name>
  <action>
User must perform these steps in the Railway dashboard (https://railway.app):
1. Create a new Railway project (or use existing)
2. Add TimescaleDB service using Docker image: `timescale/timescaledb:latest-pg15`
3. **CRITICAL**: Attach persistent volume to the database service at `/var/lib/postgresql/data` BEFORE running any migrations
4. Add environment variables to all services (copy from local .env)
5. Add RAILWAY_TOKEN to GitHub repository secrets
6. Verify railway.toml services match Railway project services
7. Test: trigger a deploy, then restart the db service, verify schema survives restart
  </action>
</task>

</tasks>

<success_criteria>
- GitHub Actions CI runs on PR: lint passes, tests pass against postgres service, docker build succeeds
- Merge to main auto-deploys all services EXCEPT rl_trainer
- Railway TimescaleDB has persistent volume at /var/lib/postgresql/data
- Schema migration survives Railway service restart (FR-1.3)
</success_criteria>
