---
phase: 01-infrastructure-data-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/script.py.mako
  - backend/alembic/versions/0001_initial_schema.py
  - backend/app/models/__init__.py
  - backend/app/models/price_bars.py
  - backend/app/models/earnings_events.py
  - backend/app/models/signals.py
  - backend/app/models/rl_transitions.py
  - backend/app/models/macro_indicators.py
  - backend/app/models/portfolio_positions.py
  - backend/app/queries/__init__.py
  - backend/app/queries/point_in_time.py
  - backend/pytest.ini
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_health.py
  - backend/tests/test_schema.py
  - backend/tests/test_as_of.py
autonomous: true
requirements:
  - FR-1.2
  - FR-1.5
must_haves:
  truths:
    - "All 6 hypertables (price_bars, earnings_events, signals, rl_transitions, macro_indicators, portfolio_positions) exist in the database after `alembic upgrade head`"
    - "Every one of the 6 tables has an `ingestion_timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW()` column"
    - "An INSERT into each hypertable succeeds and a SELECT returns the row"
    - "A query using `as_of` filtering returns only rows whose `ingestion_timestamp <= as_of`; a future-ingested row is excluded"
    - "`alembic downgrade base` followed by `alembic upgrade head` is idempotent (uses if_not_exists)"
  artifacts:
    - path: "backend/alembic/versions/0001_initial_schema.py"
      provides: "Initial migration creating all 6 hypertables with ingestion_timestamp"
      contains: "create_hypertable"
    - path: "backend/alembic/env.py"
      provides: "Alembic env with TimescaleDB include_object filter"
      contains: "_timescaledb_"
    - path: "backend/app/models/price_bars.py"
      provides: "SQLAlchemy ORM model for price_bars hypertable"
    - path: "backend/app/queries/point_in_time.py"
      provides: "as_of filter helper enforcing ingestion_timestamp <= as_of"
      contains: "ingestion_timestamp <="
    - path: "backend/tests/test_schema.py"
      provides: "Hypertable existence + insert + ingestion_timestamp column tests"
    - path: "backend/tests/test_as_of.py"
      provides: "Future-ingested row exclusion test (FR-1.5 success criterion)"
  key_links:
    - from: "backend/alembic/env.py"
      to: "backend/app/models/__init__.py"
      via: "target_metadata = Base.metadata"
      pattern: "target_metadata\\s*=\\s*Base.metadata"
    - from: "backend/alembic/env.py"
      to: "TimescaleDB internals"
      via: "include_object excludes _timescaledb_*"
      pattern: "_timescaledb_"
    - from: "backend/app/queries/point_in_time.py"
      to: "backend/app/models/price_bars.py"
      via: "filters PriceBar.ingestion_timestamp <= as_of"
      pattern: "ingestion_timestamp <="
---

<objective>
Define the canonical data schema in code and prove the point-in-time semantics work end-to-end.

Purpose: FR-1.2 requires 6 TimescaleDB hypertables (`price_bars`, `earnings_events`, `signals`, `rl_transitions`, `macro_indicators`, `portfolio_positions`); FR-1.5 requires every historical record carry `ingestion_timestamp` with `as_of` filtering enforced. Both are foundation for every later phase — wrong column name or missing filter = look-ahead bias = false Sharpe > 1.0 gate pass (NFR-1, highest-severity defect class).

Output: Alembic configured with TimescaleDB-aware env, single initial migration creating all 6 hypertables, ORM models, point-in-time query helper, and a passing test suite that proves hypertable existence, insert success, and `as_of` exclusion of future-ingested rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-data-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-data-foundation/01-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Hypertable DDL is locked from research Pattern 2. Use exact column names. -->

Hypertables and time columns:
  price_bars            -> time         (TIMESTAMPTZ), chunk_time_interval => INTERVAL '1 month'
  earnings_events       -> announced_at (TIMESTAMPTZ), chunk_time_interval => INTERVAL '3 months'
  signals               -> created_at   (TIMESTAMPTZ), chunk_time_interval => INTERVAL '1 month'
  rl_transitions        -> ts           (TIMESTAMPTZ), chunk_time_interval => INTERVAL '1 week'
  macro_indicators      -> date         (DATE),        chunk_time_interval => INTERVAL '3 months'
  portfolio_positions   -> snapshot_at  (TIMESTAMPTZ), chunk_time_interval => INTERVAL '1 month'

EVERY table has: `ingestion_timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW()` (FR-1.5 — also the as_of filter column)

create_hypertable form: `SELECT create_hypertable('<tbl>', '<time_col>', chunk_time_interval => INTERVAL '<n>', if_not_exists => TRUE)`

Base import: `from app.database import Base`
Async session for tests: `from app.database import AsyncSessionLocal, engine`

Test DB URL (override via env in conftest): `postgresql+asyncpg://pead:devpass@localhost:5432/pead_trading`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Initialize Alembic with TimescaleDB-aware env and ORM models</name>
  <files>backend/alembic.ini, backend/alembic/env.py, backend/alembic/script.py.mako, backend/app/models/__init__.py, backend/app/models/price_bars.py, backend/app/models/earnings_events.py, backend/app/models/signals.py, backend/app/models/rl_transitions.py, backend/app/models/macro_indicators.py, backend/app/models/portfolio_positions.py</files>
  <read_first>
    - .planning/phases/01-infrastructure-data-foundation/01-RESEARCH.md (Pattern 2 full DDL, Pattern 3 Alembic env, Code Examples "Alembic env.py with TimescaleDB Table Exclusion", Pitfall 2)
    - backend/app/database.py (from Plan 01-01 — exports `Base`, `engine`)
    - CLAUDE.md (line 53 — Alembic + TimescaleDB directive: exclude _timescaledb_*, hypertables via op.execute() raw SQL)
  </read_first>
  <action>
Create `backend/alembic.ini` standard layout. Set `script_location = alembic` and `sqlalchemy.url = postgresql+asyncpg://pead:devpass@localhost:5432/pead_trading` (will be overridden via env var at runtime).

Create `backend/alembic/script.py.mako` standard alembic template (copy default; nothing custom).

Create `backend/alembic/env.py` per research Code Examples "Alembic env.py with TimescaleDB Table Exclusion". MUST include:

- `def include_object(object, name, type_, reflected, compare_to)` returning False if `type_ == "table" and (name.startswith("_timescaledb_") or name.startswith("timescaledb_"))`, else True. Also exclude reflected hypertable chunks: return False if name starts with `_hyper_`.
- Async migration runner using `engine_from_config` with `poolclass=pool.NullPool` and `context.configure(connection=connection, target_metadata=target_metadata, include_object=include_object, include_schemas=False, compare_type=True)`
- `target_metadata = Base.metadata` after `from app.models import *` so all 6 models register
- Resolve URL from `os.environ.get("DATABASE_URL")` if set; fall back to alembic.ini value. Force-rewrite `postgresql+asyncpg://` -> `postgresql://` ONLY for the sync alembic codepath (alembic 1.18 supports asyncpg natively via `run_async_migrations`; use that). Use the async pattern: define `async def run_async_migrations()` that calls `engine.connect()` and uses `connection.run_sync(do_run_migrations)`. Final `if context.is_offline_mode(): run_migrations_offline() else: asyncio.run(run_async_migrations())`.

Create `backend/app/models/__init__.py` re-exporting all 6 models:
```python
from app.models.price_bars import PriceBar
from app.models.earnings_events import EarningsEvent
from app.models.signals import Signal
from app.models.rl_transitions import RlTransition
from app.models.macro_indicators import MacroIndicator
from app.models.portfolio_positions import PortfolioPosition

__all__ = ["PriceBar", "EarningsEvent", "Signal", "RlTransition", "MacroIndicator", "PortfolioPosition"]
```

Create each ORM model file matching research Pattern 2 column-for-column. Use `from app.database import Base` and SQLAlchemy 2.0 typing (`Mapped[...]`, `mapped_column(...)`). Required column sets:

`price_bars.py` — class PriceBar, __tablename__ = "price_bars":
  time TIMESTAMPTZ PK, symbol TEXT PK, open/high/low/close/vwap NUMERIC(12,4), volume BIGINT, ingestion_timestamp TIMESTAMPTZ NOT NULL default=func.now()

`earnings_events.py` — class EarningsEvent, __tablename__ = "earnings_events":
  id BIGSERIAL PK, symbol TEXT, announced_at TIMESTAMPTZ NOT NULL, fiscal_quarter TEXT, eps_actual NUMERIC(10,4), eps_estimate NUMERIC(10,4), revenue_actual NUMERIC(18,2), revenue_estimate NUMERIC(18,2), operating_income NUMERIC(18,2), share_count BIGINT, guidance_direction TEXT (CHECK constraint via __table_args__), source TEXT, ingestion_timestamp NOT NULL default=func.now(), UNIQUE(symbol, fiscal_quarter)

`signals.py` — class Signal, __tablename__ = "signals":
  created_at TIMESTAMPTZ PK default=func.now(), signal_id UUID PK default=uuid4, symbol TEXT, earnings_event_id BIGINT, eps_gap NUMERIC(8,4), quality_score NUMERIC(5,2), three_axis_composite NUMERIC(8,4), naive_position_size NUMERIC(6,4), direction TEXT (CHECK long/short/hold), status TEXT default 'pending', ingestion_timestamp NOT NULL default=func.now()

`rl_transitions.py` — class RlTransition, __tablename__ = "rl_transitions":
  ts TIMESTAMPTZ PK default=func.now(), episode_id UUID PK, step INTEGER PK, agent_id SMALLINT NOT NULL default=0, symbol TEXT, state_vec JSONB, action NUMERIC(6,4), reward NUMERIC(10,6), next_state_vec JSONB, done BOOLEAN, priority NUMERIC(10,6) default 1.0, ingestion_timestamp NOT NULL default=func.now()

`macro_indicators.py` — class MacroIndicator, __tablename__ = "macro_indicators":
  date DATE PK, series_id TEXT PK, value NUMERIC(16,6), vintage_date DATE, source TEXT, ingestion_timestamp NOT NULL default=func.now()

`portfolio_positions.py` — class PortfolioPosition, __tablename__ = "portfolio_positions":
  snapshot_at TIMESTAMPTZ PK default=func.now(), symbol TEXT PK, qty NUMERIC(12,4), avg_entry_price NUMERIC(12,4), current_price NUMERIC(12,4), unrealized_pnl NUMERIC(14,4), stop_loss_price NUMERIC(12,4), take_profit_price NUMERIC(12,4), status TEXT default 'open', ingestion_timestamp NOT NULL default=func.now()

For all models: `__table_args__ = ({"info": {"hypertable": True}},)` so the env can later detect hypertables if needed (informational only — actual hypertable conversion lives in the migration's raw SQL).
  </action>
  <verify>
    <automated>cd backend && python -c "from app.models import PriceBar, EarningsEvent, Signal, RlTransition, MacroIndicator, PortfolioPosition; from app.database import Base; tbls = sorted(Base.metadata.tables.keys()); assert tbls == ['earnings_events','macro_indicators','portfolio_positions','price_bars','rl_transitions','signals'], tbls; print('models ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "include_object" backend/alembic/env.py` AND `grep -q "_timescaledb_" backend/alembic/env.py` AND `grep -q "_hyper_" backend/alembic/env.py`
    - `grep -q "compare_type=True" backend/alembic/env.py`
    - `grep -q "target_metadata = Base.metadata" backend/alembic/env.py`
    - All 6 model files contain `ingestion_timestamp` AND `default=func.now()` (or `server_default=func.now()`)
    - Python import line `from app.models import PriceBar, EarningsEvent, Signal, RlTransition, MacroIndicator, PortfolioPosition` succeeds (model registry intact)
    - `Base.metadata.tables` contains exactly these 6 names: earnings_events, macro_indicators, portfolio_positions, price_bars, rl_transitions, signals
    - `grep -q "guidance_direction" backend/app/models/earnings_events.py`
    - `grep -q "agent_id" backend/app/models/rl_transitions.py` AND `grep -q "priority" backend/app/models/rl_transitions.py`
    - `grep -q "vintage_date" backend/app/models/macro_indicators.py` (ALFRED point-in-time column)
  </acceptance_criteria>
  <done>Alembic initialized with TimescaleDB-aware env, all 6 ORM models registered with `ingestion_timestamp`, models import cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write initial migration creating 6 hypertables + as_of query helper + tests (TDD)</name>
  <files>backend/alembic/versions/0001_initial_schema.py, backend/app/queries/__init__.py, backend/app/queries/point_in_time.py, backend/pytest.ini, backend/tests/__init__.py, backend/tests/conftest.py, backend/tests/test_health.py, backend/tests/test_schema.py, backend/tests/test_as_of.py</files>
  <read_first>
    - .planning/phases/01-infrastructure-data-foundation/01-RESEARCH.md (Pattern 2 full DDL block, Pattern 3 migration template, Pattern 7 as_of query, Validation Architecture > Phase Requirements -> Test Map, Pitfalls 2 and 4)
    - backend/app/models/*.py (this plan, Task 1)
    - backend/app/database.py (Plan 01-01)
    - CLAUDE.md (line 53 — hypertable creation via op.execute() raw SQL only)
  </read_first>
  <behavior>
    Tests (must be written first, RED, then implement migration to GREEN):
    - test_health.py::test_health_endpoint_returns_ok — FastAPI test client GET /health returns 200, body has status=ok, db=connected
    - test_schema.py::test_all_six_hypertables_exist — query `_timescaledb_catalog.hypertable` (or `timescaledb_information.hypertables`) returns rows for all 6 expected names
    - test_schema.py::test_ingestion_timestamp_columns — query information_schema.columns: every one of the 6 tables has a column named `ingestion_timestamp` with data_type `timestamp with time zone` and is_nullable='NO'
    - test_schema.py::test_hypertable_inserts — for each table, insert one row using minimal valid payload, then SELECT count(*) >= 1
    - test_schema.py::test_migration_idempotent — running `alembic upgrade head` a second time does not raise (verifies if_not_exists)
    - test_as_of.py::test_future_ingested_row_excluded — insert a price_bars row with `time = NOW() - 1 day` and `ingestion_timestamp = NOW() + 365 days`; call `get_prices_as_of(symbol="TEST", as_of=NOW())`; assert returned list is empty
    - test_as_of.py::test_past_ingested_row_included — insert a row with `ingestion_timestamp = NOW() - 1 hour`; same query returns it
  </behavior>
  <action>
RED phase — write tests first:

Create `backend/pytest.ini`:
```
[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -v --tb=short
```

Create `backend/tests/__init__.py` (empty).

Create `backend/tests/conftest.py`:
- pytest-asyncio fixtures
- `@pytest.fixture(scope="session")` event_loop returning new event loop
- `@pytest.fixture` async `db_engine` connecting to `os.environ["DATABASE_URL"]` (default `postgresql+asyncpg://pead:devpass@localhost:5432/pead_trading`)
- `@pytest.fixture` async `db` yielding `AsyncSession` from `AsyncSessionLocal()`; rollback after test
- `@pytest.fixture` `client` yielding `httpx.AsyncClient(app=app, base_url="http://test")` for FastAPI tests

Create `backend/tests/test_health.py`:
```python
import pytest
@pytest.mark.asyncio
async def test_health_endpoint_returns_ok(client):
    resp = await client.get("/health")
    assert resp.status_code == 200
    body = resp.json()
    assert body["status"] == "ok"
    assert body["db"] == "connected"
```

Create `backend/tests/test_schema.py` with the four tests in <behavior>. Use these queries:
- `SELECT hypertable_name FROM timescaledb_information.hypertables` (returns table_name)
- `SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name = :t AND column_name = 'ingestion_timestamp'`
- For inserts use minimal payloads honoring NOT NULL columns and CHECK constraints (e.g., `guidance_direction='none'`, `direction='long'`).

Create `backend/tests/test_as_of.py` with the two tests in <behavior>, calling `from app.queries.point_in_time import get_prices_as_of`.

Run tests — they MUST fail (no migration yet). Commit: `test(01-02): add failing tests for hypertables and as_of filtering`.

GREEN phase — write the migration and the query helper:

Create `backend/app/queries/__init__.py` (empty).

Create `backend/app/queries/point_in_time.py` per research Pattern 7:
```python
from datetime import datetime, timedelta
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select
from app.models.price_bars import PriceBar

async def get_prices_as_of(
    db: AsyncSession,
    symbol: str,
    as_of: datetime,
    lookback_days: int = 90,
) -> list[PriceBar]:
    stmt = (
        select(PriceBar)
        .where(PriceBar.symbol == symbol)
        .where(PriceBar.time >= as_of - timedelta(days=lookback_days))
        .where(PriceBar.time <= as_of)
        .where(PriceBar.ingestion_timestamp <= as_of)
        .order_by(PriceBar.time.desc())
    )
    result = await db.execute(stmt)
    return list(result.scalars().all())
```

Create `backend/alembic/versions/0001_initial_schema.py`:
- revision = "0001", down_revision = None
- `upgrade()`:
  1. `op.execute("CREATE EXTENSION IF NOT EXISTS timescaledb CASCADE")`
  2. For each of the 6 tables, `op.execute(""" CREATE TABLE IF NOT EXISTS <name> ( ... ingestion_timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(), PRIMARY KEY (...) ) """)` matching research Pattern 2 column-for-column.
  3. For each table, `op.execute("SELECT create_hypertable('<name>', '<time_col>', chunk_time_interval => INTERVAL '...', if_not_exists => TRUE)")` IMMEDIATELY after CREATE TABLE (Pitfall 4 — before any inserts).
  4. Create the listed indexes from Pattern 2.
- `downgrade()`: `DROP TABLE IF EXISTS <name> CASCADE` for each in reverse order, then `DROP EXTENSION IF EXISTS timescaledb CASCADE`.

Run migration against the running stack from Plan 01-01:
```bash
cd backend
DATABASE_URL=postgresql+asyncpg://pead:devpass@localhost:5432/pead_trading alembic upgrade head
```

Run the test suite — all tests MUST pass. Commit: `feat(01-02): create initial schema with 6 hypertables and as_of filter`.

REFACTOR (only if needed): factor repeated DDL into helper. Tests must still pass.
  </action>
  <verify>
    <automated>cd backend && DATABASE_URL=postgresql+asyncpg://pead:devpass@localhost:5432/pead_trading alembic upgrade head && DATABASE_URL=postgresql+asyncpg://pead:devpass@localhost:5432/pead_trading pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "create_hypertable" backend/alembic/versions/0001_initial_schema.py` — appears at least 6 times
    - `grep -q "if_not_exists => TRUE" backend/alembic/versions/0001_initial_schema.py` — Pitfall 4 mitigation
    - `grep -q "CREATE EXTENSION IF NOT EXISTS timescaledb" backend/alembic/versions/0001_initial_schema.py`
    - `grep -q "ingestion_timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW()" backend/alembic/versions/0001_initial_schema.py` — appears 6 times
    - `grep -q "ingestion_timestamp <=" backend/app/queries/point_in_time.py`
    - `pytest backend/tests/test_health.py -v` — all pass
    - `pytest backend/tests/test_schema.py::test_all_six_hypertables_exist -v` — passes
    - `pytest backend/tests/test_schema.py::test_ingestion_timestamp_columns -v` — passes
    - `pytest backend/tests/test_schema.py::test_hypertable_inserts -v` — passes
    - `pytest backend/tests/test_schema.py::test_migration_idempotent -v` — passes (running alembic upgrade head twice does not error)
    - `pytest backend/tests/test_as_of.py::test_future_ingested_row_excluded -v` — passes (FR-1.5 success criterion)
    - `pytest backend/tests/test_as_of.py::test_past_ingested_row_included -v` — passes
    - `psql ... -tAc "SELECT count(*) FROM timescaledb_information.hypertables"` returns 6
  </acceptance_criteria>
  <done>All 6 hypertables created via Alembic migration, every table has NOT NULL ingestion_timestamp DEFAULT NOW(), as_of helper excludes future-ingested rows, full pytest suite green. FR-1.2 and FR-1.5 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration -> DB schema | Static SQL in migration files; no user input |
| as_of query parameter -> SQL | as_of value bound via SQLAlchemy parameter (no string concat) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | alembic/versions/0001_initial_schema.py | mitigate | Migrations static, no user-supplied values; op.execute() uses literal DDL only |
| T-02-02 | Information Disclosure | as_of query | mitigate | Parameterized via SQLAlchemy `where(... <= as_of)` — no f-string interpolation |
| T-02-03 | Tampering | look-ahead bias from missing as_of filter | mitigate | NFR-1 highest-severity defect class; test_future_ingested_row_excluded enforces filter exists |
| T-02-04 | Denial of Service | massive insert into rl_transitions | accept | Phase 5 concern; rate limited at app layer in later phase |
</threat_model>

<verification>
- `alembic upgrade head` succeeds against the running TimescaleDB
- `alembic upgrade head` is idempotent (run twice without error)
- `pytest backend/tests/` is fully green
- `SELECT count(*) FROM timescaledb_information.hypertables` returns 6
- Every one of the 6 tables has `ingestion_timestamp` NOT NULL with DEFAULT NOW()
</verification>

<success_criteria>
- All 6 required hypertables exist (FR-1.2)
- `ingestion_timestamp` column on every table (FR-1.5)
- `as_of` query excludes future-ingested rows (FR-1.5)
- Migration uses raw `op.execute()` for hypertable creation (CLAUDE.md directive)
- TimescaleDB internal tables excluded from autogenerate (Pitfall 2)
- Test suite covers all FR-1.2 and FR-1.5 success criteria
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-data-foundation/01-02-SUMMARY.md`
</output>
