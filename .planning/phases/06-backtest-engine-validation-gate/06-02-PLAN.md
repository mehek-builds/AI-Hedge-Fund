---
phase: "06"
plan: "02"
name: production-code-reuse-wiring
type: execute
autonomous: true
wave: 1
depends_on: ["06-01"]
requirements: [FR-6.2]
---

# Phase 6 Plan 2: Production-Code Reuse Wiring

## Objective

Verify and extend FR-6.2 compliance: `replay.py` must call into production signal engine,
SAC ensemble, and portfolio sizing code exclusively. No backtest-only reimplementations.
Add the missing must-have invariant tests for portfolio sizing and position update logic.

## Context

- @.planning/phases/06-backtest-engine-validation-gate/06-CONTEXT.md (plan breakdown, FR-6.2 must-haves)
- @.planning/phases/06-backtest-engine-validation-gate/06-01-SUMMARY.md (what Wave 1 built)
- @backend/app/backtest/replay.py (production wiring, verify FR-6.2 compliance)
- @backend/tests/backtest/test_backtest_uses_prod_engine.py (FR-6.2 tests, extend for missing invariants)

## Tasks

### Task 1: Verify replay.py FR-6.2 compliance

**Type:** auto

**Description:**
Review `replay.py` to confirm all production imports are present and no signal/sizing logic
is re-implemented. Specifically verify:
- `compute_signal_for_event` imported from `app.signals.pipeline` (not defined locally)
- `load_macro_snapshot` imported from `app.portfolio.macro_loader`
- `SACEnsemble` and `MoEController` imported from `rl.*`
- `simulate_fill` imported from `app.backtest.fills` (not signal logic)
- No `def compute_signal*`, `def eps_gap`, or `def compute_position_size` defined locally

If any production import is missing or any logic is re-implemented, fix it.

**Done criteria:**
- replay.py imports only from production modules for signal/RL/portfolio logic
- No function definitions that duplicate production signal or portfolio code
- ruff lint passes on replay.py

### Task 2: Extend FR-6.2 test coverage for missing must-have invariants

**Type:** auto

**Description:**
The existing `test_backtest_uses_prod_engine.py` covers signal pipeline and RL imports.
Add the missing must-have invariant tests:

1. `test_no_compute_position_size_defined_in_backtest_modules` -- portfolio sizing not re-implemented
2. `test_no_portfolio_pipeline_logic_in_backtest_modules` -- no portfolio pipeline reimplementation
3. `test_single_definition_of_compute_signal_for_event` -- exactly one definition exists in production
4. `test_replay_imports_fills_from_production_module` -- fills imported from app.backtest.fills, not reimplemented
5. `test_no_select_action_defined_in_backtest_modules` -- SAC select_action not re-implemented

These tests use AST inspection (same pattern as existing tests).

**Done criteria:**
- 5 new test functions added to TestNoParallelSignalImplementation
- All new tests pass
- Test count goes from 6 to 11

### Task 3: Lint and full test run

**Type:** auto

**Description:**
Run ruff check and ruff format --check on all modified files.
Run the full backtest test suite to confirm all tests pass.

**Done criteria:**
- `cd backend && ruff check . && ruff format --check .` passes clean
- `cd backend && python3 -m pytest tests/backtest/ -v --tb=short` shows all tests pass (at least 60 passed)

## Verification

- FR-6.2: import-graph test passes with no backtest-only signal/RL/portfolio code
- All tests in tests/backtest/ pass (55+ tests)
- ruff lint clean

## Success Criteria

- [ ] replay.py verified FR-6.2 compliant
- [ ] 5 new must-have invariant tests added and passing
- [ ] ruff lint passes
- [ ] All backtest tests pass
