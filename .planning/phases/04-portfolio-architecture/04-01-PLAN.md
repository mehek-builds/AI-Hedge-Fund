---
phase: 04-portfolio-architecture
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/portfolio/__init__.py
  - backend/app/portfolio/macro.py
  - backend/app/portfolio/caps.py
  - backend/app/portfolio/risk.py
  - backend/tests/portfolio/__init__.py
  - backend/tests/portfolio/test_macro.py
  - backend/tests/portfolio/test_caps.py
  - backend/tests/portfolio/test_risk.py
autonomous: true
requirements:
  - FR-4.1
  - FR-4.2
  - FR-4.3
  - FR-4.4
  - FR-4.6
must_haves:
  truths:
    - "Macro composite score in [-6, 0] is computed deterministically from the 6 components"
    - "Sizing multiplier maps macro score correctly: 1.0x for [0, -1], 0.65x for [-2, -3], 0.25x for [-4, -6]"
    - "When E/P < real TIPS 10Y, all position sizes are multiplied by 0.80"
    - "Any Mag-7 position > 3% NAV is capped to exactly 3% and the cap is logged"
    - "Stop-loss triggers at exactly 8% drawdown from entry, independent of any other sizing logic"
  artifacts:
    - path: "backend/app/portfolio/macro.py"
      provides: "compute_macro_score(), apply_sizing_multiplier(), MACRO_BANDS"
    - path: "backend/app/portfolio/caps.py"
      provides: "MAG7, apply_mag7_cap(), apply_erp_cap(), CapDecision"
    - path: "backend/app/portfolio/risk.py"
      provides: "STOP_LOSS_THRESHOLD, stop_loss_triggered(), stop_loss_price()"
  key_links:
    - from: "backend/app/portfolio/macro.py"
      to: "MACRO_BANDS dict"
      via: "apply_sizing_multiplier(score) returns Decimal multiplier"
      pattern: "MACRO_BANDS"
    - from: "backend/app/portfolio/caps.py"
      to: "MAG7 set"
      via: "apply_mag7_cap(symbol, size_nav) checks symbol in MAG7"
      pattern: "MAG7"
---

<objective>
Build the four pure-computation primitives that gate every position size in Phase 4: macro composite scoring + sizing multiplier (FR-4.1, FR-4.2), ERP compression cap (FR-4.3), Mag-7 concentration cap (FR-4.4), and 8% stop-loss enforcement (FR-4.6).

Purpose: Phase 4 layers risk controls on top of the Phase 3 signal pipeline. Each control must be a deterministic, DB-free, Decimal-precise function that can be unit-tested in isolation and composed in Plan 02. Pattern mirrors `backend/app/signals/` from Phase 3.

Output: Three pure-computation modules under `backend/app/portfolio/` plus three test modules — no DB, no Celery, no scipy dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-signal-engine/03-02-SUMMARY.md
@backend/app/signals/composite.py
@backend/app/signals/filters.py
@backend/app/models/macro_indicators.py

<interfaces>
<!-- Patterns to mirror from Phase 3 -->

From backend/app/signals/composite.py (style reference — clamping + Decimal arithmetic):
```python
def valuation_score(eps_gap: Optional[Decimal], max_eps_gap: Decimal) -> Decimal:
    # ... returns Decimal in [0, 100] with rounding to 4dp
```

From backend/app/signals/filters.py (style reference — (passed: bool, reason: str) tuples):
```python
def apply_sector_hurdle(quality_score: int, sector: str) -> tuple[bool, str]: ...
```

From backend/app/models/macro_indicators.py (existing schema — read-only here):
- (date, series_id) primary key
- value: Numeric(16, 6)
- vintage_date: ALFRED point-in-time release date
- ingestion_timestamp: FR-1.5 column
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Macro composite scorer + sizing multiplier (FR-4.1, FR-4.2)</name>
  <files>backend/app/portfolio/__init__.py, backend/app/portfolio/macro.py, backend/tests/portfolio/__init__.py, backend/tests/portfolio/test_macro.py</files>
  <read_first>
    - backend/app/signals/composite.py (Decimal arithmetic + rounding pattern)
    - backend/app/signals/filters.py (tuple return pattern)
    - backend/app/models/macro_indicators.py (schema this code reads)
  </read_first>
  <behavior>
    - score_component(name, value): returns 0 or -1 per published thresholds (yield_curve: -1 if T10Y2Y < 0; sahm: -1 if SAHMREALTIME >= 0.50; lei: -1 if 6m change < 0; ism_pmi: -1 if MANEMP yoy < 0; hyg_lqd_spread: -1 if spread > 4.5; jpy_aud_carry: -1 if carry < 0); else 0
    - compute_macro_score(components: dict[str, Optional[Decimal]]) -> int: sum of 6 component scores, clamped to [-6, 0]; missing component contributes 0 (not -1)
    - apply_sizing_multiplier(score: int) -> Decimal: 1.0 for score in {0, -1}; 0.65 for score in {-2, -3}; 0.25 for score in {-4, -5, -6}; ValueError for score > 0 or < -6
    - MACRO_BANDS exported as: {(0, -1): Decimal("1.0"), (-2, -3): Decimal("0.65"), (-4, -6): Decimal("0.25")}
    - Test 1: score=0 -> 1.0; score=-1 -> 1.0
    - Test 2: score=-2 -> 0.65; score=-3 -> 0.65
    - Test 3: score=-4 -> 0.25; score=-5 -> 0.25; score=-6 -> 0.25
    - Test 4: score=1 raises ValueError; score=-7 raises ValueError
    - Test 5: compute_macro_score with all 6 negative -> -6; all 6 positive -> 0
    - Test 6: compute_macro_score with 3 negative + 3 positive -> -3
    - Test 7: missing components contribute 0, not -1 (e.g. {} returns 0)
    - Test 8: score_component thresholds: yield_curve=-0.01 -> -1; yield_curve=0.0 -> 0; sahm=0.50 -> -1; sahm=0.49 -> 0
  </behavior>
  <action>
    Create `backend/app/portfolio/__init__.py` (empty module docstring) and `backend/app/portfolio/macro.py`.

    Hardcode these constants exactly:
    ```python
    from decimal import Decimal
    MACRO_BANDS: dict[tuple[int, int], Decimal] = {
        (0, -1): Decimal("1.0"),
        (-2, -3): Decimal("0.65"),
        (-4, -6): Decimal("0.25"),
    }
    COMPONENT_NAMES = ("yield_curve", "sahm", "lei", "ism_pmi", "hyg_lqd_spread", "jpy_aud_carry")
    ```

    Function signatures:
    - `def score_component(name: str, value: Optional[Decimal]) -> int` — returns 0 if value is None or threshold not breached, -1 if breached. Raise ValueError on unknown name.
    - `def compute_macro_score(components: dict[str, Optional[Decimal]]) -> int` — iterates COMPONENT_NAMES, sums score_component for each (missing keys treated as None -> 0). Result clamped to [-6, 0].
    - `def apply_sizing_multiplier(score: int) -> Decimal` — band lookup; raise ValueError(f"score {score} out of [-6, 0]") for out-of-range.

    Use only `decimal.Decimal` for thresholds and return values (no float). Module docstring must reference FR-4.1 and FR-4.2.

    Tests in `backend/tests/portfolio/test_macro.py` follow the 8 cases in `<behavior>`. Mirror Phase 3 test structure (one file per module, function-style tests with descriptive names like `test_score_minus_two_returns_065`). No DB fixtures.

    RED first: write all 8 tests, run pytest, confirm all fail with ImportError or AttributeError. Commit as `test(04-01): add failing tests for macro composite scorer`.
    GREEN: implement to pass. Commit as `feat(04-01): implement macro score and sizing multiplier (FR-4.1, FR-4.2)`.
  </action>
  <verify>
    <automated>cd backend && pytest tests/portfolio/test_macro.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - File exists: backend/app/portfolio/macro.py
    - File exists: backend/app/portfolio/__init__.py
    - File exists: backend/tests/portfolio/test_macro.py
    - `grep -E "MACRO_BANDS\s*=" backend/app/portfolio/macro.py` matches
    - `grep -E "def compute_macro_score" backend/app/portfolio/macro.py` matches
    - `grep -E "def apply_sizing_multiplier" backend/app/portfolio/macro.py` matches
    - `grep -E "Decimal\(\"0.65\"\)" backend/app/portfolio/macro.py` matches
    - `grep -E "Decimal\(\"0.25\"\)" backend/app/portfolio/macro.py` matches
    - `grep -E "Decimal\(\"1.0\"\)" backend/app/portfolio/macro.py` matches
    - `pytest backend/tests/portfolio/test_macro.py` returns exit 0 with >= 8 tests passing
    - No `import` of sqlalchemy, fastapi, celery, or scipy in macro.py
  </acceptance_criteria>
  <done>All 8+ tests pass; module is import-safe with no DB or external deps; MACRO_BANDS dict matches domain constants exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mag-7 cap + ERP compression cap (FR-4.3, FR-4.4)</name>
  <files>backend/app/portfolio/caps.py, backend/tests/portfolio/test_caps.py</files>
  <read_first>
    - backend/app/portfolio/macro.py (Decimal style established in Task 1)
    - backend/app/signals/filters.py (tuple-return pattern for constraint events)
  </read_first>
  <behavior>
    - MAG7 frozenset: {"AAPL", "MSFT", "NVDA", "GOOGL", "GOOG", "META", "AMZN"}
    - MAG7_CAP = Decimal("0.03")  # 3% NAV
    - ERP_CAP_MULTIPLIER = Decimal("0.80")
    - apply_mag7_cap(symbol: str, size_nav: Decimal) -> CapDecision: if symbol in MAG7 and size_nav > 0.03 -> capped=0.03, was_capped=True; else passthrough
    - apply_erp_cap(size_nav: Decimal, ep_yield: Decimal, real_tips_yield: Decimal) -> CapDecision: if ep_yield < real_tips_yield -> size_nav * 0.80, was_capped=True; else passthrough
    - CapDecision is a frozen dataclass: (size_nav: Decimal, was_capped: bool, reason: str)
    - Test 1: AAPL with 0.05 NAV -> capped to 0.03, was_capped=True, reason includes "MAG7"
    - Test 2: AAPL with 0.025 NAV -> 0.025, was_capped=False
    - Test 3: NVDA with exactly 0.03 -> 0.03, was_capped=False (boundary)
    - Test 4: AAPL with 0.0301 -> 0.03, was_capped=True (just over boundary)
    - Test 5: Non-Mag-7 symbol "JPM" with 0.10 NAV -> 0.10, was_capped=False
    - Test 6: GOOGL and GOOG both treated as Mag-7
    - Test 7: ERP cap applied when ep_yield=0.04 and tips_yield=0.05 -> size * 0.80
    - Test 8: ERP cap NOT applied when ep_yield=0.06 and tips_yield=0.05 -> passthrough
    - Test 9: ERP cap boundary: ep_yield == tips_yield -> NOT capped (strict `<`)
    - Test 10: Symbol case-insensitive ("aapl" treated as Mag-7)
  </behavior>
  <action>
    Create `backend/app/portfolio/caps.py`. Hardcode constants exactly:
    ```python
    MAG7: frozenset[str] = frozenset({"AAPL", "MSFT", "NVDA", "GOOGL", "GOOG", "META", "AMZN"})
    MAG7_CAP: Decimal = Decimal("0.03")
    ERP_CAP_MULTIPLIER: Decimal = Decimal("0.80")
    ```

    Frozen dataclass:
    ```python
    @dataclass(frozen=True)
    class CapDecision:
        size_nav: Decimal
        was_capped: bool
        reason: str  # empty string when was_capped=False
    ```

    Function signatures:
    - `def apply_mag7_cap(symbol: str, size_nav: Decimal) -> CapDecision` — uppercase the symbol before set membership check.
    - `def apply_erp_cap(size_nav: Decimal, ep_yield: Decimal, real_tips_yield: Decimal) -> CapDecision` — strict `<` comparison (ep_yield < real_tips_yield triggers cap).

    Constraint logging: when `was_capped=True`, the calling site (Plan 02 pipeline) is responsible for logging via `log.warning(...)`. This module returns the decision object; it does NOT log directly. Add a docstring note: "Caller logs `CapDecision` as a constraint event when was_capped is True."

    RED first: write all 10 tests, confirm fail. Commit as `test(04-01): add failing tests for Mag-7 and ERP caps`.
    GREEN: implement. Commit as `feat(04-01): implement Mag-7 concentration cap and ERP compression cap (FR-4.3, FR-4.4)`.
  </action>
  <verify>
    <automated>cd backend && pytest tests/portfolio/test_caps.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - File exists: backend/app/portfolio/caps.py
    - `grep -E "MAG7\s*[:=]\s*frozenset" backend/app/portfolio/caps.py` matches
    - `grep -E '"AAPL".*"MSFT".*"NVDA"' backend/app/portfolio/caps.py` OR all 7 symbols present (verify by `grep -oE '"(AAPL|MSFT|NVDA|GOOGL|GOOG|META|AMZN)"' backend/app/portfolio/caps.py | sort -u | wc -l` returns 7)
    - `grep -E 'MAG7_CAP.*Decimal\(\"0.03\"\)' backend/app/portfolio/caps.py` matches
    - `grep -E 'ERP_CAP_MULTIPLIER.*Decimal\(\"0.80\"\)' backend/app/portfolio/caps.py` matches
    - `grep -E "class CapDecision" backend/app/portfolio/caps.py` matches
    - `grep -E "def apply_mag7_cap" backend/app/portfolio/caps.py` matches
    - `grep -E "def apply_erp_cap" backend/app/portfolio/caps.py` matches
    - `pytest backend/tests/portfolio/test_caps.py` returns exit 0 with >= 10 tests passing
    - No DB or external deps imported in caps.py
  </acceptance_criteria>
  <done>All 10+ tests pass; MAG7 frozenset has exactly 7 tickers; both caps return CapDecision with correct boundary semantics (Mag-7 strict `>`, ERP strict `<`).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 8% stop-loss enforcement (FR-4.6)</name>
  <files>backend/app/portfolio/risk.py, backend/tests/portfolio/test_risk.py</files>
  <read_first>
    - backend/app/portfolio/caps.py (Decimal + frozen dataclass pattern)
    - backend/app/models/portfolio_positions.py (entry_price field naming)
  </read_first>
  <behavior>
    - STOP_LOSS_THRESHOLD = Decimal("0.08")  # 8% drawdown
    - stop_loss_price(entry_price: Decimal, direction: str) -> Decimal:
      - direction="long" -> entry * (1 - 0.08)
      - direction="short" -> entry * (1 + 0.08)
      - else -> ValueError
    - stop_loss_triggered(entry_price: Decimal, current_price: Decimal, direction: str) -> bool:
      - long: drawdown = (entry - current) / entry; True iff drawdown >= 0.08
      - short: drawdown = (current - entry) / entry; True iff drawdown >= 0.08
      - hold/invalid -> ValueError
    - Test 1: long entry=100, current=92.00 -> triggered=True (exactly 8%)
    - Test 2: long entry=100, current=92.01 -> triggered=False (just under)
    - Test 3: long entry=100, current=91.99 -> triggered=True (over)
    - Test 4: short entry=100, current=108.00 -> triggered=True (exactly 8%)
    - Test 5: short entry=100, current=107.99 -> triggered=False
    - Test 6: long entry=100, current=100 -> triggered=False
    - Test 7: long entry=50, current=46 -> triggered=True (8% of 50 = 4)
    - Test 8: stop_loss_price(entry=100, "long") -> Decimal("92.00")
    - Test 9: stop_loss_price(entry=100, "short") -> Decimal("108.00")
    - Test 10: direction="hold" raises ValueError on both functions
    - Test 11: entry_price <= 0 raises ValueError
    - Test 12: stop trigger is independent of any RL or sizing input — pass arbitrary direction/price, no other state needed
  </behavior>
  <action>
    Create `backend/app/portfolio/risk.py`. Hardcode:
    ```python
    STOP_LOSS_THRESHOLD: Decimal = Decimal("0.08")
    ```

    Function signatures:
    - `def stop_loss_price(entry_price: Decimal, direction: str) -> Decimal`
    - `def stop_loss_triggered(entry_price: Decimal, current_price: Decimal, direction: str) -> bool`

    Use Decimal arithmetic exclusively. Compute drawdown to full Decimal precision (no float). For comparison with 0.08, use `>=` (exact 8% triggers — see Test 1 and FR-4.6 spec "triggers at exactly 8%").

    Module docstring must say: "Stop-loss is enforced independently of RL sizing recommendations (FR-4.6). This module has no dependency on position sizing — it only takes entry/current prices and direction."

    RED first. Commit as `test(04-01): add failing tests for 8% stop-loss enforcement`.
    GREEN. Commit as `feat(04-01): implement 8% stop-loss enforcement (FR-4.6)`.
  </action>
  <verify>
    <automated>cd backend && pytest tests/portfolio/test_risk.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - File exists: backend/app/portfolio/risk.py
    - `grep -E 'STOP_LOSS_THRESHOLD.*Decimal\(\"0.08\"\)' backend/app/portfolio/risk.py` matches
    - `grep -E "def stop_loss_price" backend/app/portfolio/risk.py` matches
    - `grep -E "def stop_loss_triggered" backend/app/portfolio/risk.py` matches
    - `pytest backend/tests/portfolio/test_risk.py` returns exit 0 with >= 12 tests passing
    - `pytest backend/tests/portfolio/test_risk.py -k "exactly_8"` runs and passes (boundary test for FR-4.6 success criterion 6)
    - No imports of sqlalchemy, scipy, numpy, or any RL-related module in risk.py
  </acceptance_criteria>
  <done>All 12+ tests pass; 8% boundary triggers exactly per FR-4.6; module is provably independent of any sizing logic (no sizing-related imports).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> portfolio module | Caller (Plan 02 pipeline) supplies position sizes, prices, macro values; module assumes types but validates ranges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | macro.py inputs | mitigate | compute_macro_score validates score is int in [-6, 0]; missing components default to 0 (fail-safe: no risk-on bias from missing data) |
| T-04-02 | Tampering | caps.py size inputs | mitigate | apply_mag7_cap and apply_erp_cap return CapDecision with explicit was_capped flag — caller cannot silently skip the cap |
| T-04-03 | Information disclosure | constraint events | accept | Cap events log symbol + size; no PII, internal-only logs |
| T-04-04 | Repudiation | stop-loss bypass | mitigate | risk.py has zero imports from sizing/RL modules; cannot be silently disabled by upstream changes |
| T-04-05 | Denial of service | macro band lookup | accept | All inputs validated in O(1) lookups; no unbounded loops |
</threat_model>

<verification>
After all 3 tasks pass individually:

```bash
cd backend && pytest tests/portfolio/ -v
```

Expected: 30+ tests pass (8 macro + 10 caps + 12 risk minimum), 0 fail, 0 skip. All tests run offline without DB.

Confirm pure-computation discipline:
```bash
grep -rE "(sqlalchemy|fastapi|celery|scipy|numpy)" backend/app/portfolio/
```
Expected: no matches (Plan 02 introduces scipy in completion.py only).
</verification>

<success_criteria>
- All 3 modules exist under backend/app/portfolio/
- All 3 test modules exist under backend/tests/portfolio/
- 30+ unit tests pass with `pytest tests/portfolio/`
- No DB, scipy, numpy, FastAPI, or Celery imports in any Plan 01 module
- Domain constants match spec exactly (MAG7 set, MACRO_BANDS, ERP 0.80, stop 0.08)
- 6 commits total: 3 RED + 3 GREEN, one per task
</success_criteria>

<output>
After completion, create `.planning/phases/04-portfolio-architecture/04-01-SUMMARY.md` covering modules built, test counts, decisions, and threat mitigations applied.
</output>
