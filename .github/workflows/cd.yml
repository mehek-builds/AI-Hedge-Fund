name: CD

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy FastAPI
        run: railway up --service fastapi --detach
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

      - name: Deploy Celery worker
        run: railway up --service celery_worker --detach
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

      - name: Deploy Prefect server
        run: railway up --service prefect_server --detach
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

      - name: Deploy Next.js frontend
        run: railway up --service nextjs --detach
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

      # rl_trainer is intentionally excluded — it requires manual Railway deploy
      # to prevent auto-deploy from killing in-progress training jobs.
      # Deploy rl_trainer manually via: railway up --service rl_trainer
